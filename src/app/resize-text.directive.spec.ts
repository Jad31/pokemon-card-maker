import { ResizeTextDirective } from './resize-text.directive';

describe('ResizeTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeTextDirective();
    expect(directive).toBeTruthy();
  });
});
